<ContentDialog
    x:Class="eComBox.Views.WhatsNewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Windows.UI.Xaml"
    
xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    x:Uid="WhatsNewDialog"
    d:DesignHeight="345"
    d:DesignWidth="550"
    CornerRadius="5"
    mc:Ignorable="d">
    <ContentDialog.TitleTemplate>
        <DataTemplate x:DataType="x:String">
            <StackPanel Orientation="Horizontal">
                <Image
                    Width="40"
                    Height="40"
                    Source="ms-appx:///Assets/StoreLogo.png" />
                <TextBlock
                    Margin="{StaticResource SmallLeftMargin}"
                    VerticalAlignment="Center"
                    Text="{x:Bind}" />
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>
    <ScrollViewer>
        <StackPanel>
            <RichTextBlock >
                <Paragraph>
                    <Run FontWeight="Bold">已更新到 eComBox 0.3.3 版本</Run>
                </Paragraph>
                <Paragraph></Paragraph>
                <Paragraph>新的“设置”页面；</Paragraph>
                
                <Paragraph>添加了页面宽度上限；</Paragraph>
                <Paragraph>添加了日期计数器的导入导出功能；</Paragraph>
                <Paragraph>修复了对话框不能适配暗色主题的问题；</Paragraph>
                <Paragraph>修复了一些其他问题。</Paragraph>
            </RichTextBlock>
            <HyperlinkButton Content="隐私政策"  NavigateUri="https://ecombox.ecomter.site/docs/privacy" Margin="{StaticResource XSmallTopMargin}" />
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
